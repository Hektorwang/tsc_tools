# 检查对时配置

## 功能说明

检查对时配置, 当系统配置的对时计划任务不可用, 或本机授时服务开启, 或本机配置了多条 ntpdate 计划任务则告警

## 使用方法

```bash
# 从工具主入口调度
./run.sh -m 模块名
# 从模块私有入口调度
CONF_FILE="配置文件" RESULT_DIR="结果输出目录" WARN_DIR="告警日志目录" log_file="日志文件" modules/模块名/run.sh
```
