# 系统服务状态检查

## 功能说明

检查系统服务状态. 若系统初始化程序为 SysV, 则提示开机启动服务; 若系统初始化程序为 systemd, 则检测服务设置的开机启动状态和运行状态不同告警.

## 使用方法

```bash
# 从工具主入口调度
./run.sh -m 模块名
# 从模块私有入口调度
CONF_FILE="配置文件" RESULT_DIR="结果输出目录" WARN_DIR="告警日志目录" log_file="日志文件" modules/模块名/run.sh
```
