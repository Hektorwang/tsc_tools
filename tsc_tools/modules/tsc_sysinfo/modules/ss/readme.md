# 采集网络连接信息

## 功能说明

采集网络连接信息 `ss -sanoptux`. 对于打开 TCP 监听的程序, 如果程序使用的文件描述符数量超过系统级或用户级或进程级文件描述符配置上限的 80% 则告警.

## 使用方法

```bash
# 从工具主入口调度
./run.sh -m 模块名
# 从模块私有入口调度
CONF_FILE="配置文件" RESULT_DIR="结果输出目录" WARN_DIR="告警日志目录" log_file="日志文件" modules/模块名/run.sh
```
